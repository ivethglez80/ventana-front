version: '3.9'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    command: pnpm run dev
    ports:
      - '${APP_PORT}:3000'
    env_file:
      - .env
